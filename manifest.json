{
  "author": "CandleSmartHome.com",
  "description": "Let your Raspberry Pi generate a wifi hotspot. Connect your wifi-based smart home devices to this hotspot to stop them from connecting to the internet without your permission.",
  "content_scripts": [
    {
      "css": [
        "css/extension.css"
      ],
      "js": [
        "js/extension.js"
      ]
    }
  ],  
  "gateway_specific_settings": {
    "webthings": {
      "exec": "python3 {path}/main.py",
	  "primary_type": "adapter",
      "strict_max_version": "*",
      "strict_min_version": "0.10.0"
    }
  },
  "homepage_url": "https://www.candlesmarthome.com/hotspot",
  "id": "hotspot",
  "license": "MPL-2.0",
  "manifest_version": 1,
  "name": "Hotspot",
  "options": {
    "default": {
	  "Hotspot name":"Candle",
	  "Hotspot password":"iloveprivacy",
	  "Time server": true,
	  "Multicast relay": false,
	  "Use blocklist": true,
	  "Skip network check": false,
      "Debugging": false
    },
    "schema": {
      "properties": {
        "Hotspot name": {
            "description": "The name of the wifi hotspot.",
            "type": "string"
        },
        "Hotspot password": {
            "description": "The password for the wifi hotspot.",
            "type": "string",
			"writeOnly": true
        },
        "Use blocklist": {
          "description": "Enabling this offers additional built-in protection against behaviour tracking of your devices. This is done by blocking access to specific data collection servers that companies may use, based on lists with IP addresses of such servers.",
          "type": "boolean"
        },
        "Time server": {
          "description": "Your devices may want to incessantly ask for the time to various time servers, effectively revealing your IP address to those server. If you enable this option, all those requests will be handled by this server intead, through some redirection.",
          "type": "boolean"
        },
        "Skip network check": {
          "description": "Advanced. If enabled, your Candle Controller will no longer check if it has a netwok connection when it boots up. If your controller has no connection, then the Hotspot addon will still create its own wifi signal. Connecting to that will then be your only way to access the gateway (or until you connect a network cable). The upside is that it allows the controller to start even if there is no other network router available.",
          "type": "boolean"
        },
        "Debugging": {
          "description": "Advanced. Debugging allows developers to diagnose any issues with the add-on.",
          "type": "boolean"
        }
      },
      "required": [
	    "Hotspot name",
		"Hotspot password"
      ],
      "type": "object"
    }
  },
  "short_name": "hotspot",
  "version": "0.1.4",
  "web_accessible_resources": [
    "css/*.css",
    "images/*.svg",
	"images/*.png",
	"images/*.gif",
    "js/*.js",
    "views/*.html"
  ]
}
